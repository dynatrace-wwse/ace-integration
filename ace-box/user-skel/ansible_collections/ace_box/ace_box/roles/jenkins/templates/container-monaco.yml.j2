              - containers:
                - args: "cat"
                  command: "/bin/sh -c"
                  image: "{{ jenkins_monaco_container_image }}"
                  livenessProbe:
                    failureThreshold: 0
                    initialDelaySeconds: 0
                    periodSeconds: 0
                    successThreshold: 0
                    timeoutSeconds: 0
                  name: "monaco"
                  ttyEnabled: true
                  workingDir: "/home/jenkins/agent"
                label: "monaco-runner"
                name: "monaco-runner"
                slaveConnectTimeout: 300
                slaveConnectTimeoutStr: "300"
                yamlMergeStrategy: "override"
              - containers:
                - args: "cat"
                  command: "/bin/sh -c"
                  image: "{{ jenkins_monaco_v2_container_image }}"
                  livenessProbe:
                    failureThreshold: 0
                    initialDelaySeconds: 0
                    periodSeconds: 0
                    successThreshold: 0
                    timeoutSeconds: 0
                  name: "monaco"
                  ttyEnabled: true
                  workingDir: "/home/jenkins/agent"
                label: "monaco-v2-runner"
                name: "monaco-v2-runner"
                slaveConnectTimeout: 300
                slaveConnectTimeoutStr: "300"
                yamlMergeStrategy: "override"
